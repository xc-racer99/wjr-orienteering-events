Shortcode Usage
[wjr-fullcalendar topClubId clubIds appendClubName eventColoring]
where
	topClubId is the top club level ID to start with for the club selection page (optional, default 76 which is Orienteering Canada (OC))
	clubIds is a comma seperated list of WhyJustRun club IDs (optional, 0 for all clubs in the WJR database, default 76 which is OC)
	appendClubName is a boolean value determining if event names are pre-pended (optional, default true)
	eventColoring is the method by which the events are colored (eventType for filtering based on Club/National, eventSeries for the color defined on WJR, club for Club name (optional, default eventType)
